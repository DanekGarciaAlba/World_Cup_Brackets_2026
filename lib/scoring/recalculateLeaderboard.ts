export async function recalculateLeaderboard() {
  return {
    ok: false,
    message: "Leaderboard recalculation is scaffolded; full scoring sync comes after schema connection is confirmed.",
  };
}
