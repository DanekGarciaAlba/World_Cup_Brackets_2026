# Changelog

## 2026-06-11

- Implemented protected live-pick/profile visibility additions in a safe non-OneDrive workspace after Codex crash recovery. `/picks` match cards now get an additive red live state for `live`/`halftime`, keep the existing gold final state, show a top-right point-audit info button for locked signed-in fixtures, and toast when a signed-in user attempts to save/unlock after live lock. Added an authenticated own-user point-audit endpoint backed by existing `match_predictions` and `prediction_scores` metadata, extended `/api/dashboard/score-state` with recent scored matches, and updated the score popup to acknowledge scored games by match/result/points so old games do not re-popup after recalculation. Public profiles now show a scrollable `Predictions` timeline over synced playable fixtures, auto-position near the current relevant fixture, and still sanitize hidden future scores server-side before sending data to the browser. Added focused tests for profile focus and score-popup acknowledgement keys. Verified in the safe copy with `npm.cmd run test -- tests/predictionVisibility.test.ts tests/scoreAcknowledgement.test.ts`, `npm.cmd run lint`, and `npm.cmd run test`; build/browser checks were intentionally not run in this crash-recovery pass to avoid stressing the laptop again.
- Hardened public match-pick privacy for the "hide picks until deadline" task. Public profile prediction previews now flow through `lib/privacy/publicPredictionPreview.ts`, which only reveals saved scores and saved timestamps at or after the fixture kickoff lock, and treats invalid kickoff data as not revealable for privacy even though invalid kickoff data still locks editing. Added `tests/predictionVisibility.test.ts` coverage for before-lock hiding, exact-lock reveal, malformed-score non-coercion, and invalid-kickoff non-reveal. Added `docs/CODEX_STABILITY.md` so future Codex sessions can recover context without broad scans over OneDrive-heavy generated folders. Verified with focused privacy test, `npm.cmd run test`, and `npm.cmd run lint`; `npm.cmd run build` was intentionally not run in this crash-recovery pass to avoid expanding `.next` while OneDrive is already under memory pressure.
- Completed a finished-match scoring/lock readiness pass for launch day. Cron sync now detects newly finished or corrected API-Football fixture results before upserting matches and triggers `recalculateLeaderboard()` only when a finished result changes, so match scoring, group-completion bracket scoring, Top 8 completion scoring, daily highlights, ranked players, clubs, and user stat caches refresh after real results arrive.
- Kept finished pick fixtures visible on `/picks` by rendering from all confirmed synced fixtures instead of only future fixtures. Finished match cards now lock in a gold final-score state using official `home_score` / `away_score`, while the user's saved prediction remains separate below the card.
- Added a lightweight signed-in score celebration overlay plus `/api/dashboard/score-state`. The client stores a per-user local baseline and shows a fast points/correct/exact delta pop-up when cached scoring increases, including when a user returns after points changed.
- Re-audited pick lock and bracket-drop fairness logic: save/delete still use the server kickoff lock, group prediction point values still drop by each group's first two kickoff times, group predictions do not score until that group's six fixtures are finished, and Top 8 third-place scoring still waits until all 12 groups are complete.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run test:api-football`, `npm.cmd run perf:audit --if-present`, a read-only Supabase/API-Football snapshot, and local Browser desktop/mobile smoke checks on `/demo/picks` with no horizontal overflow. Current live data has 48 teams, 72 scheduled confirmed group fixtures, latest cron success at `2026-06-11T13:03:33Z`, next kickoff `2026-06-11T19:00:00Z`, and no finished matches yet; World Cup validation remains warning-only because official knockout fixtures are provider-pending.

## 2026-06-10

- Fixed club activity wording and opened-club roster display. Add-by-email and join-by-code activity now names the actual club, and old `joined the club` rows are normalized when read so the live Activity Feed shows messages like `mugre joined IT Team :).` without a production data rewrite. Club leaderboard rows now keep the existing top-3 preview chips but also carry a full `members` roster; opened club panels on the shared Rivals/Leaderboard club card now list every visible member instead of only the top three. Deployed production `dpl_C4q2KXbhNY63YfxUu2zizbPkXE21` to `https://world-cup-brackets-2026.vercel.app`. Verified with `npm.cmd run lint`, focused `tests/clubs.test.ts`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, Vercel inspect, production HTTP smokes, Vercel error/500 log scans, and a read-only Supabase check confirming `IT Team :)` has 4 members, a 3-member preview, a 4-member opened roster, and club-named activity messages. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added simple club management for the live Rivals flow. Club owners can open Manage, remove non-owner members, and delete the club; regular members can leave their club. The server-side club APIs enforce owner/member permissions, block owner removal through the member-delete path, clear primary-club pointers when needed, use existing cascade deletion for deleted clubs, insert leave/remove activity for surviving clubs, and rebuild ranked-club cache after every club mutation so club rankings and highlights refresh. The Rivals card keeps the existing layout, shows Add by email only for owners, and uses a compact Manage dialog with non-destructive loading state. Deployed production `dpl_58N9nBHtJSRsy9JD5ntQKPU7PFnS` to `https://world-cup-brackets-2026.vercel.app`. Verified with `npm.cmd run lint`, focused `tests/clubs.test.ts`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, local and production HTTP smokes for `/login`, `/rivals`, unauthenticated club DELETE guards, authenticated Browser QA on `/rivals`, Vercel inspect, and Vercel error/500 log scans. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Rebuilt the in-app scoring guide UI without changing scoring logic: `/picks` now opens a readable responsive `Pick Points` modal with exact-score, timing, stage, lock, privacy, and fairness rules from the launch guide; `/bracket` now has a compact `Bracket guide` trigger with a tabbed modal for bracket logic and all Group A-L point-drop times from `world-cup-brackets-2026-group-points-timeline.pdf`. The modal layout no longer uses the old narrow grid that caused unreadable horizontal overflow. Deployed production `dpl_9XB3DJaMJ2pt6Wp2qDHXGaAvc8UP` to `https://world-cup-brackets-2026.vercel.app`. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, local desktop/mobile Browser modal geometry checks, Vercel inspect, production `/login` and `/demo/picks` HTTP 200 smokes, production `/bracket` auth redirect smoke, a production Browser check for the public Points guide modal, and Vercel log scans. The log scan found no 500s; it found one non-crashing stale Supabase refresh-token auth log from the browser smoke request, which still returned HTTP 200. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Removed the unwanted last launch-note section from `artifacts/launch-docs/world-cup-brackets-2026-guide.tex` so the guide now ends after the User FAQ. Recompiled `artifacts/launch-docs/world-cup-brackets-2026-guide.pdf` with bundled Tectonic, copied the refreshed PDF to `C:/Users/User/Downloads/world-cup-brackets-2026-guide.pdf`, refreshed the email attachment copy, and rebuilt `artifacts/email/tgp-world-cup-bracket-outlook-email-package.zip`. Verified the removed section text is absent from both the project PDF and Downloads PDF; compile completed with the same non-blocking underfull box warnings in existing table text.
- Fixed app-wide displayed kickoff/deadline timestamps to render in Eastern time (`America/New_York`) while leaving countdown, lock, scoring, and UI layout logic unchanged. Dashboard, picks checklist, bracket deadlines/drop labels, profile reveal dates, activity timestamps, and related saved-time labels now share the same Eastern formatter; the first synced kickoff `2026-06-11T19:00:00Z` renders as `Jun 11, 3:00 PM`. Added a focused formatter regression test and deployed production `dpl_ERWnVjyARekk59nzmDxwPUQAb1W2` to `https://world-cup-brackets-2026.vercel.app`. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, Vercel inspect, production `/login` HTTP 200 smoke, and a live Supabase next-kickoff display check.
- Rebuilt the paste-ready Outlook launch email document at `artifacts/email/tgp-world-cup-bracket-outlook-email.docx` again using only the latest user-provided clipboard screenshots. The corrected document includes six cropped, email-sized visuals for dashboard, picks, bracket, leaderboard, clubs, and locked public picks; it skips the blurry modal and avoids a mascot-heavy profile promo. The rewrite removes the false firm-deadline language, removes future separate-email/link wording, keeps the same internal sports-fan tone, and describes the app as an internal points-only pool. Copied the two launch PDFs beside it as `artifacts/email/TGP-World-Cup-Brackets-2026-Guide.pdf` and `artifacts/email/TGP-World-Cup-Group-Stage-Rules-and-Points.pdf`, and recreated `artifacts/email/tgp-world-cup-bracket-outlook-email-package.zip` containing the DOCX plus both PDFs. Verified the DOCX reopens with 6 inline images and the production app hyperlink, verified both PDF copies exist, and scanned the email text for the prior bad phrases plus restricted gambling/betting terms with no hits. DOCX render QA could not be completed because local LibreOffice/Word renderers were unavailable.
- Added `admin@tgp.build` and `danek.chat@tgp.build` to the exact system-admin/full-cosmetic/competitive-hidden email list while keeping the existing `danek.chat@gmail.com` admin. Leaderboard recalculation now deletes all system-admin leaderboard cache rows instead of only checking the old single admin email. Cleared the live `danek.chat@gmail.com` leaderboard cache row that showed `66` points / rank `#1`, zeroed its `user_stats_cache` points/rank, and rebuilt ranked-club cache with admin users excluded. Deployed production `dpl_6XsxvNp4nF5XHXFrhmn2eyw2z7CE` to `https://world-cup-brackets-2026.vercel.app`; verified with `npm.cmd run lint`, focused auth test, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, Vercel inspect, production `/login` HTTP 200 smoke, and live Supabase row checks.
- Fixed the live launch signup blocker caused by Supabase email confirmation. The signup server action now uses the server-only Supabase admin API to create allowed `@tgp.build` users with `email_confirm: true`, then signs them in normally, so new users do not need confirmation emails. The rescue path confirms already-created but unconfirmed accounts and updates the submitted password before sign-in; confirmed existing accounts are told to use Sign in. Confirmed the already-stuck `guest@tgp.build` account and verified there are now `0` unconfirmed `@tgp.build` users. Deployed production `dpl_CNRQPPzeg95ZGBm8tPDfq3npCXBb` to `https://world-cup-brackets-2026.vercel.app`; verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, Vercel inspect, and production `/login` HTTP 200 smoke.
- Created a separate LaTeX mini-guide for group-stage point timing: `artifacts/launch-docs/world-cup-brackets-2026-group-points-timeline.tex` plus compiled PDF, then completed a FIFA official-source pass and compiled `artifacts/launch-docs/world-cup-brackets-2026-group-points-timeline-fifa-verified.pdf` after the original PDF filename was locked by another process. It lists every Group A-L scoring change with exact Eastern and UTC timestamps, the point value after each change, and the match/cutoff that triggers it. Verified the schedule against synced Supabase fixture data, the app's group scoring/deadline helpers, and FIFA's official FWC26 match schedule PDF dated 10 April 2026; all 24 first/second group kickoff timestamps matched. Ran `npm.cmd run validate:worldcup`; validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Revised the launch guide to clarify bracket timing and save behavior: Groups, Top 8, and Knockout are separate timing windows; locked segments preserve previous valid saved picks; and rearranging teams or clicking winners only changes the draft until `Save groups`, `Save Top 8`, or `Save knockout` succeeds. Removed user-facing 3D/model references from the launch guide and recompiled the PDF.
- Created the launch documentation package in LaTeX: `artifacts/launch-docs/world-cup-brackets-2026-guide.tex` plus compiled PDF, and `artifacts/launch-docs/world-cup-brackets-2026-newsletter.tex` plus compiled PDF. The guide documents the full user/admin experience, scoring rules, cosmetic unlocks, privacy/reveal timing, release timeline, infrastructure, and launch readiness; the newsletter includes subject lines, preview text, full company intro copy, short Slack/Teams copy, and announcement-owner checklist. Verified both with the bundled Tectonic LaTeX compiler; the guide only has non-blocking underfull table-cell warnings.
- Completed the final production readiness pass for the June 11 release. Added the supplied North America trophy/flags image as the transparent `/leaderboard` arena background, added the crown-stadium background treatment to `/picks`, strengthened leaderboard button glow, and changed unranked/zero-rank leaderboard/profile snippets to display `-` instead of `0`; bracket UX was left untouched.
- Deployed production deployment `dpl_3gWygbY8jYNUkvoRmTKqT5yR9KbC` and aliased it to `https://world-cup-brackets-2026.vercel.app`. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, Supabase health/data/RLS/index/advisor checks, Vercel inspect/log checks, protected-route HTTP smokes, production asset smokes, in-app Browser QA for `/leaderboard` and `/picks`, and a 100-request production `/login` burst smoke. No production 500/error logs were found after deployment; World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Implemented admin visibility and cosmetic unlock polish: `danek.chat@gmail.com` is the exact system-admin/full-cosmetic account, Admin nav is hidden for everyone else, and competitive leaderboards/search/highlights/ranked-club scoring exclude the hidden admin while preserving admin personal stats for profile display.
- Reworked cosmetic unlocks to lifetime point thresholds plus automatic tournament dates instead of spendable coins. Added the first-run starter mascot chooser, point/date/final-week unlock metadata for mascots, skins, emotes, and pins, a Player Pass points pill, vertical full-width skin/emote rows, locked-card requirements, and a cleaner filled preview card. Verified with `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Completed a launch-readiness scoring/privacy/data audit for the June 11 release path. Fixed Top 8 third-place scoring so leaderboard recalculation ignores API-Football's special `Ranking of third-placed teams` standings table and only derives actual third-place candidates from official Group A-L standings rows. Fixed the Vitest Windows alias path so `@/*` imports resolve during tests. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, Supabase health/index/RLS/data checks, Vercel inspect/log/metrics checks, and a 100-request production `/login` smoke. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Deployed the profile persistence fix to production after hosted testing showed the production alias was still on a stale 2026-06-09 deployment. New production deployment `dpl_EZ6z3xBaTQyE2Aw7LKujYNuTiPUY` is aliased to `https://world-cup-brackets-2026.vercel.app`.
- Reproduced the mascot save flow through the real local UI: selecting Zayu submitted `mascot_id=zayu`, saved `selected_mascot=zayu`, `avatar_base=zayu-starter`, and `celebration_style=zayu-starter-thumbs-up` in Supabase, then stayed Zayu after navigating to `/leaderboard` and back to `/profile/setup`.
- Verified the hosted alias points at the new deployment with `vercel.cmd inspect`; the automation browser could not inspect the signed-in production profile page because it had no `vercel.app` session cookie and correctly redirected to login.
- Fixed profile cosmetic persistence reads so public profiles and leaderboard/profile surfaces no longer recompute mascot state in a way that can reject a saved non-default mascot and fall back to Maple. The locker read path now merges stored Supabase unlocks with rule-derived unlocks and keeps the saved selected mascot visible.
- Updated profile and mascot saves to preserve stored unlocked mascots and revalidate the connected dashboard, leaderboard, profile setup, public profile, and rivals routes after a save so profile changes propagate across the app.
- Added focused mascot locker-state tests for saved public mascots and stored unlock preservation. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, and read-only in-app Browser checks for `/profile/setup`, `/leaderboard`, and `/profile/[userId]`. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Changed leaderboard/dashboard player profile links to use the saved emote inside the circular profile mark, matching the app shell Profile nav treatment instead of the speech-bubble emote style.
- Polished `/picks` match cards with two roomier team score panels, clearer `Pick locked` / `Unsaved update` / `Missing pick` states, and a green locked-in button state once a pick is saved.
- Added an unlock confirmation for saved match picks explaining that removing the pick clears its saved time and may reduce timing points if the user saves again later; the existing save/delete API and connected checklist/leaderboard update flow remain intact.
- Applied the existing V2 Supabase migration `points_system_v2_timestamps` to the remote project after live lock-pick QA exposed missing `meaningful_updated_at` / advancer timestamp columns; verified a real save changed the checklist to `1/72`, showed the green `Pick locked in` state, opened the unlock warning, and returned to `0/72` after cleanup.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and `npm.cmd run perf:audit --if-present`. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Polished bracket group point-window cards: each group now shows compact 1st/2nd/3rd available points plus a group-specific countdown for the next point drop, with shorter date copy that fits inside the card. Verified with `npm.cmd run lint`, `npm.cmd run build`, local `3137` route smoke, and in-app Browser DOM/visual checks.
- Hid finalized tournament paths on profile surfaces until the derived knockout bracket lock deadline, including the public clicked profile and the `/profile/setup` public-view panel.
- Hid public match score picks until each fixture reaches its kickoff pick deadline; unrevealed clicked-profile rows now show a locked state without exposing the saved score or saved timestamp.
- Replaced leaderboard placeholder avatars/coming-soon states with clickable public-profile links using each user's saved emote bubble across player leaderboards, club member rows, and dashboard leaderboard snippets.
- Changed the large Player Pass preview slot to render the selected mascot's lightweight mascot card regardless of selected skin, avoiding 3D/model requests and skin-dependent blank-card fallback behavior.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, and local HTTP smokes. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Revised the Remotion promo again to add the missing daily-picks moment and give the generated kits a stronger showcase. The 20-second video now uses six tighter chapters: intro, daily picks, kit locker, bracket, leaderboard, and dashboard/clubs.
- Added a new daily-picks board with match cards, score picks, saved/draft/missing states, kickoff lock copy, and points-only scoring language. Added a new kit locker board using the high-detail generated profile kit PNGs for Canada, Mexico, USA, Brazil, and France as full-uniform hero cards.
- Tightened promo copy to sound less generic, deepened the frame-driven 3D title entrance without letting it cover product UI, reduced chapter overlap to avoid clutter, and moved `promo:still` to frame 345 so the default still lands in the bracket chapter.
- Re-rendered `artifacts/promos/world-cup-2026-promo.mp4` as a refreshed 20-second 1080p MP4 at about 9.9 MB, with new daily-picks and kits stills. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, direct Remotion still renders at frames 151, 250, 448, and 545, `npm.cmd run promo:still`, and `npm.cmd run promo:render`. Remotion still warns about `zod@4.4.3` versus expected `zod@4.3.6`; World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Polished the Remotion promo again after visual feedback: the Round of 32/bracket chapter now uses a captured app bracket surface at `public/assets/promo/bracket-top32-glow.png`, cropped and brightened so the top-32/team-card imagery matches the app's glowy navy/gold bracket UI instead of the earlier mock tiles.
- Added restrained 3D-style title entrances using Remotion frame-driven perspective/scale transforms, softened chapter transitions with light overlap/crossfades, and removed duplicate bracket callout clutter inside the product screenshot.
- Re-rendered `artifacts/promos/world-cup-2026-promo.mp4` as a refreshed 20-second 1080p MP4 at about 9.9 MB, plus refreshed bracket and leaderboard stills. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, direct Remotion still renders at frames 300 and 420, `npm.cmd run promo:still`, and `npm.cmd run promo:render`. Remotion still warns about `zod@4.4.3` versus expected `zod@4.3.6`; World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Revised the Remotion promo after visual feedback: reduced clutter, removed overlapping scene timing, simplified the persistent header, gave the leaderboard/points section its own chapter, and reframed dashboard/clubs as two large end cards.
- Re-rendered `artifacts/promos/world-cup-2026-promo.mp4` as a cleaner 20-second 1080p MP4 at about 8.3 MB, plus refreshed stills for the bracket, leaderboard, and dashboard/clubs sections.
- Verified the promo revision with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, direct Remotion still renders at frames 390 and 510, `npm.cmd run promo:still`, and `npm.cmd run promo:render`. Remotion still warns about `zod@4.4.3` versus expected `zod@4.3.6`; World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added a Remotion-based 20-second 1080p promo video for newsletter/internal launch use. The new `WorldCup2026Promo` composition lives under `remotion/` and spotlights the app's mascots, skins, premium UI, bracket builder, points, dashboard, leaderboard, and clubs.
- Added `promo:studio`, `promo:still`, and `promo:render` scripts. Rendered output is saved at `artifacts/promos/world-cup-2026-promo.mp4`, with sampled stills in the same folder.
- Kept the promo separate from production app behavior: no API-Football browser calls, no Supabase writes, no production GLB loading, and all copy stays free and points-only with no betting/odds/money mechanics.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, `npm.cmd run promo:still`, extra Remotion still samples at frames 165 and 570, and `npm.cmd run promo:render`. Remotion completed the still/video renders but warns that the app has `zod@4.4.3` while Remotion's version check expects `zod@4.3.6`; World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Polished `/login` into a calmer standalone auth gate: compact World Cup/TGP context, a single account-access panel, clearer sign-in copy, a cleaner create-account form, and a subdued forgot-password note.
- Replaced the client-tabbed Sign in/Create account switch with server-rendered URL modes (`/login` and `/login?mode=signup`) so account creation works reliably without waiting on tab hydration.
- Kept the existing auth rules unchanged: users still sign in through Supabase, signup still appends `@tgp.build`, approved admin accounts remain sign-in only, and no password recovery data is stored.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, and in-app Browser QA on the existing `localhost:3000` dev server. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.

## 2026-06-09

- Polished the V2 bracket/picks workflow: bracket stages now show live timer cards for group cutoff, Top 8 open/lock, and knockout lock; Top 8 navigation is gated by the real timer; group rows have visible up/down controls in addition to drag; knockout shows missing picks by round; `/picks` has a live missing-pick checklist plus Saved/Missing card status.
- Verified the timed workflow polish with `npm.cmd run lint` and `npm.cmd run build`.
- Implemented World Cup Points System V2 locally: match picks now use strict scheduled-kickoff locks, latest meaningful edit timestamps, knockout advancer support, no-negative V2 scoring, and integer basis-point timing/stage multipliers.
- Split tournament prediction timing into independent segments: per-group meaningful timestamps with dynamic group scoring windows, Top 8 third-place open/lock/timing rules, and no-early-bird team-progression knockout bracket scoring.
- Added `supabase/migrations/20260609213000_points_system_v2_timestamps.sql` for V2 timestamp columns and indexes. The matching remote Supabase migration was applied on 2026-06-10.
- Updated `/picks` to load all confirmed future fixtures with real teams only, added knockout advancer selection to match cards, added group-card point chips plus Top 8/knockout deadline states in the bracket flow, and refreshed the points guide copy to the V2 rules.
- Verified V2 with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and `npm.cmd run perf:audit --if-present`. World Cup validation remains warning-only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added safe launch password UX on `/login`: password fields now have show/hide controls, signup shows a temporary current-entry mask while typing, and the forgot-password panel avoids email sends while directing users to admin reset. No password characters are stored or revealed after signup.
- Added a safe approved-admin exception to the login/app gate: normal Supabase auth is still required, ordinary non-`@tgp.build` emails are still blocked, and the existing full-cosmetic admin email plus configured `ADMIN_EMAILS` can enter without adding a secret no-auth backdoor or hardcoded password. Added unit coverage for the email gate.
- Fixed the personal `/profile/setup` editor selection/save path: mascot and kit changes now refresh the live preview, emote choices stay scoped to the selected skin, full-cosmetic access is honored in the emote picker, and profile/avatar/mascot saves use checked admin writes instead of silently ignoring Supabase errors. Verified with `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Finalized the dashboard presentation geometry requested for the live demo: real `/dashboard` and `/demo` now share an even 3x2 card board with Ranked Players / Top 3 In My Club / Top 10 Predictions Today on the top row and My Predictions / League Highlights / Your Avatar on the bottom row, with fixed demo-card sizing and internal scrolling.
- Fixed profile kit selection persistence so picking a jersey in the active profile setup flow also updates the dashboard avatar kit preview via the same local kit id used by the dashboard picker.
- Hardened Clubs/Rivals flows: club detail now requires owner/member access before returning rosters, emails, or invite codes; add-by-email search now reads signed-in profiles directly so zero-point users can be found; join/add membership insert races now return 409 conflicts for DB cap or duplicate cases instead of raw server errors.
- Polished club joining UX with successful create/join/add dialogs closing after refresh, a copy-code button for invite-code sharing, and click-to-add email search results in the add-member dialog.
- Tightened club and dashboard scoring edge cases: ranked clubs de-dupe accidental duplicate membership rows, averages use the same top-10 scoring set as club points, and the Top 10 Predictions Today fallback now stays on one latest matchday instead of mixing older scored dates.
- Verified Supabase and Vercel readiness for the clubs/leaderboard path with live read-only metadata checks for RLS, indexes, policies, and the 10-member DB trigger; Vercel project inspection confirmed the linked production deployment is READY, the production URL returned HTTP 200, and no production error/fatal logs were found in the last 24 hours. Note: the current every-minute cron schedule requires a Vercel plan that supports once-per-minute cron.
- Re-verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and `npm.cmd run perf:audit --if-present`. World Cup validation remains `WARNING` only because 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added the app auth gate in `proxy.ts`: Supabase session cookies are refreshed before render, signed-out users are redirected to `/login`, and signed-in users with non-`@tgp.build` emails are blocked from core app routes.
- Simplified `/login` into a standalone gate outside the app shell, added a fixed `@tgp.build` suffix for account creation, and added a clear wrong-domain state for existing sessions.
- Updated desktop and mobile app chrome so valid signed-in users see `Signed in` plus sign out instead of a stale `Sign in` prompt.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, signed-out `/dashboard` redirect smoke checks, and in-app Browser QA for a non-`@tgp.build` session. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Audited and tightened the points/lock fairness path: daily match picks now score zero at the exact kickoff instant, invalid kickoff data locks rather than opens, and full tournament path saves/deletes are rejected after the first synced tournament kickoff.
- Hardened group scoring so group predictions only score after that group's six fixtures are finished, and the +2 third-place qualifier bonus only pays once all 12 groups are complete and the actual top-eight third-place teams are resolvable without a cutoff tie on available standings criteria.
- Removed the active match-pick boost toggle from the real picks card because boost flags are not part of the scoring math, updated the points guide copy, and added unit coverage for exact-kickoff, exact-deadline, invalid-lock, and third-place non-qualifier edge cases.
- Gated admin pages plus the admin data-quality/system-check APIs with `requireAdmin()` before using service-role-backed Supabase reads or returning backend diagnostics. Verified RLS is enabled on the core public tables and prediction/admin-sensitive policies are scoped to `authenticated` users.
- Verified the audit with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and `npm.cmd run perf:audit --if-present`. Validation remains `WARNING` only because 72 of 104 fixtures are synced and official knockout fixtures are still provider-pending.
- Rebuilt the clicked public profile view into the requested two-column layout: the left side now stacks the existing public profile card, corrected kit card, and full finalized bracket path, while the right side adds skinny rank/points/correct/club stat cards and a four-match predictions panel using existing flag assets and saved user match picks.
- Wired real `/profile/[userId]` prediction previews from the next four synced fixtures plus saved `match_predictions`, updated the demo profile route to the same public-view structure, and added stable CSS geometry for the public profile grid so the layout fits without relying on fragile arbitrary Tailwind columns. Verified with `artifacts/profile-preview/public-profile-click-layout.png`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Replaced the real dashboard hierarchy with the stronger demo-style layout while preserving real cached data: the hero now includes rank/today/total/exact stats plus next pick deadline and next match name/time, the first dashboard row uses Top 10 Ranked Players / cropped Top 3 In My Club / Top 10 Predictions Today, and the existing kit card sits directly beneath the club top-3 card.
- Polished dashboard/demo cards with stadium-backed surfaces, glowing trophy-gold rank/point numbers, and visible time labels on daily prediction highlight rows. Verified with local Chrome screenshots plus `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, and HTTP 200 route smokes for `/`, `/dashboard`, and `/demo`.
- Corrected the demo/real dashboard layout pass: `/demo` now replaces the selected-presenter card with the command-center card, both real and demo dashboards keep the avatar/kit card flush beneath the club top-3 card, and the old lower real-dashboard cards are removed except My Predictions. The dashboard kit picker now saves clicked kit choices immediately so the jersey preview updates and persists locally.
- Tightened `ProfileKitCard` kit-art composition so the supported-country card keeps the same square size and bottom-left country/kit label placement while showing the full uniform render instead of a cropped jersey close-up. Verified with a Chrome screenshot plus `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Implemented the points, leaderboard, dashboard add-ons, and Clubs/Rivals sprint: daily picks now use no-negative timing multiplier scoring from the latest edit time, bracket/group picks use segment timing eligibility, prediction score metadata stores timing/base/final audit data, and the leaderboard rebuild now refreshes leaderboard, user stats, daily highlights, and ranked club caches.
- Added the safe Supabase migration for normalized profile email, primary club, bracket/group prediction metadata, prediction score metadata, leaderboard today points, user stats, daily highlights, ranked clubs, mini-league member roles, indexes, RLS cache reads, and DB max-10 club enforcement.
- Added dashboard summary, leaderboard players/clubs, user search, club search, and clubs create/join/add-member/detail APIs; wired dashboard add-on cards, Players/Clubs leaderboard tabs, working Clubs/Rivals UI, and `/groups` redirect while preserving the existing dashboard structure.
- Added focused scoring and club unit coverage for timing buckets, no-negative scoring, bracket segment eligibility, and top-10 club ranking math. Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, and route smokes for `/dashboard`, `/leaderboard`, `/leaderboard?tab=clubs`, `/rivals`, and `/groups`.
- Added the safe Profile 3D mascot asset pipeline and thumbnail-first preview adapter: source assets are copied to `C:\WorldCup3D_Work\source`, processed one at a time into `public/models/mascots/{mascotId}/{skinId}/`, checkpointed in `reports/`, and kept as `THUMBNAIL_ONLY` until lab approval.
- Added `assets:3d:audit`, `assets:3d:smoke`, `assets:3d:optimize`, `assets:3d:overnight`, `assets:3d:resume`, and `assets:3d:report` scripts with memory/disk gates, Blender/gltf-transform processing, generated `profile-3d-manifest.json`, and exact task file tracking in `reports/3d-files-touched.md`.
- Integrated `Profile3DPreviewSlot` into the existing left Player Pass slot without replacing the skin catalog, save flow, unlock logic, or 2D fallback. Profile initial load stays GLB-free; approved 3D models lazy-load one selected `low.glb` only.
- Processed all 12 mascot skin sources through the safe pipeline. The 2-asset smoke passed, all entries remain `THUMBNAIL_ONLY`, and verification passed with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and one safe `npm.cmd run build`. Browser QA confirmed the unauthenticated profile redirect path makes zero GLB requests and mounts zero canvases; authenticated click-load QA still needs a signed-in session and approved asset.
- Added the provided Defaults assets as real Starter skins and three Starter emotes for Maple, Zayu, and Clutch, and added the provided premium Canada/Mexico/USA mascot card images to the mascot picker.
- Reworked skin rarity/access rules to use Starter, Rare, Epic, and Legendary; Maple Classic Home, Zayu Verde Sombrero, and Clutch Stars Top Hat now show as Legendary, with normal users limited to Starter cosmetics by default and `danek.chat@gmail.com` retaining full cosmetic access.
- Fixed the live profile-nav emote preview so selecting a different skin-scoped emote in the profile editor updates the circular Profile icon immediately and is not overwritten by a late saved-profile fetch.
- Added profile-specific trimmed kit thumbnails under `public/assets/kits/custom/profile/` and updated profile kit cards to use contained kit art plus a bottom-left shiny gold kit label.
- Verified the starter/default cosmetic and kit-card pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, asset HTTP/alpha checks, demo public-profile screenshot verification, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added a clearly marked demo environment at `/demo`, `/demo/profile/[userId]`, and `/demo/picks?user=...` with 10 fake users, fake leaderboard stats, profile pins, mascot skins/emotes, kits, winner picks, and read-only fake match-day picks layered on real synced fixtures.
- Built the demo profile click-through so each fake leaderboard user opens a premium public-style profile card with featured pin, pinned row, points breakdown, kit card, winner prediction, and pick preview without writing fake data to Supabase.
- Verified the demo environment with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, route HTTP checks, and Chrome desktop/mobile visual checks confirming no horizontal overflow.
- Regenerated all 27 profile emote PNGs as transparent 512x512 cutouts, removing the baked checkerboard/white boxes so emotes fit cleanly in speech bubbles and circular profile marks.
- Enlarged profile emotes slightly in the public-card bubble and desktop/mobile nav circle, brightened the mascot locker background, and made the profile/kit preview pair equal square cards with contained kit artwork.
- Added total points and correct-prediction counts directly to the full public profile card, and compacted profile finalized-path cards to include champion, finalists, semi-finalists, quarter-finalists, third-place winner, and Top 8 thirds without internal scrolling.
- Verified the emote/profile fit polish with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, unauthenticated `/profile/setup` redirect smoke, profile emote asset HTTP/alpha checks, plus profile-path scans for restricted terms and 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Replaced the visible dashboard `My Groups` card with `My Winner Prediction`, backed by the signed-in user's saved tournament champion and a flag/pending state link to `/bracket`.
- Replaced the visible dashboard `Bracket Health` card with `Correct Predictions`, backed by `leaderboard_cache.correct_outcomes`, and removed the active `Pick Status` and `Data Integrity` cards from the dashboard.
- Verified the dashboard card replacement with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and Chrome checks for `/dashboard` and `/` confirming Winner Prediction / Group Activity / Correct Predictions with no removed card labels or mobile horizontal overflow.
- Increased the public-card emote art slightly inside the speech-bubble badge while keeping it contained, and added a circular saved-emote profile mark for the desktop/mobile Profile nav item with a palette-backed background instead of a black square.
- Enlarged profile kit artwork within its bounded card frame so jerseys read better without overlapping the country label.
- Densified the profile finalized-path card so champion, finalists, semi-finalists, and all 8 quarter-finalists fit in the fixed block without internal scrolling.
- Verified the readability polish with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, unauthenticated `/profile/setup` redirect smoke, profile asset HTTP checks, plus profile-path scans for restricted terms and 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Polished the active profile setup preview: reduced the compact public card footprint, replaced the hard square emote overlay with a speech-bubble badge that contains the emote image without face-cropping, and replaced overlapping supported-country art with a flush `ProfileKitCard` layout.
- Added `public/assets/profile/cosmetic-arena-bg.png` from the provided blue arena reference and wired it behind the mascot/skin/kit/emote customization panel; added `public/assets/profile/emote-speech-bubble-frame.png` as the transparent profile emote badge frame.
- Added palette-aware glow styling to profile skin options so each skin uses its own accent colors for border, background, and selected glow.
- Added an exact-email full-cosmetic-access override for `danek.chat@gmail.com` in `lib/profile/cosmeticAccess.ts`, and wired it into mascot locker reads, profile saves, mascot selection saves, and pin unlock/save guards.
- Verified the profile polish with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, `npm.cmd run perf:audit --if-present`, profile route/asset HTTP checks, plus profile-path scans for restricted terms and 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Integrated the Canada, Mexico, and USA profile skin/emote repos into web-sized app assets under `public/assets/profile/`, with exactly three skin cards per mascot and three matching emotes per skin.
- Replaced the active skin/emote catalogs with image-backed entries, scoped the emote picker to the selected skin, reset emotes safely on mascot/skin changes, and hardened profile saves/public rendering against mismatched old emote ids.
- Updated the square public profile card to render the selected skin artwork with the selected emote as the top-right badge, and trimmed profile finalized-path panels so the champion/finalists/semi-finalists/quarter-finalists fit without being cut off.
- Verified the asset integration with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated `/profile/setup` redirect smoke, login and asset HTTP checks, plus profile-path scans for restricted terms and 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Fixed the Germany and Argentina custom kit cutouts by rebuilding the PNGs from the original clean artwork colors with a tighter opaque matte, removing the smoky gray bleed on white fabric against dark stadium cards.
- Regenerated `public/assets/ui/kit-library-full.webp` so the shared kit banner uses the corrected Germany and Argentina whites.
- Reworked the profile preview system around the clicked-profile format: added square `FullPublicProfileCard` and emote-only `PublicProfileAvatar` primitives, changed the setup top-right area to equal square profile/kit cards, and made `/profile/[userId]` lead with the large square public card plus kit and finalized path.
- Enlarged the active finalized-path areas by removing profile/kit header clutter and internal bracket-card scrolling from the active setup/public profile cards.
- Verified the profile card pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated `/profile/setup` and login route smoke checks, plus profile-path scans for restricted terms and 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Polished `/bracket` group rank badges so 1st is glowing trophy gold, 2nd is glowing green, and 3rd is glowing bronze.
- Tightened Top 8 flag chips so the flag frame has no padding and the flag fills the frame flush.
- Browser-verified rank badge computed colors/glows, Top 8 flag geometry, and no horizontal overflow; verified again with `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Removed the visible spin preview, kit number, duplicate emote/loadout cards, and Exact/Skin/Emote clutter from the profile UI. The setup profile card now keeps one small emote badge in the top-right of the profile tile, puts the selected kit in the freed right-side space, and keeps the left preview to Pts/Rank only.
- Verified the cleanup with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated `/profile/setup` and login route smoke checks, plus profile-path scans for restricted terms and 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Centered the `/bracket` floating drag preview on the pointer so the country card no longer hangs off to the side while reordering group teams.
- Browser-verified visible Group A drag reorder after the centering tweak, then verified with `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Rebuilt the weak profile identity section around a new lightweight `ProfilePortrait` component so the profile badge, selected skin, kit flag, and emote read as one polished player identity instead of repeated cropped mascot boxes.
- Reworked the setup and public profile supported-country panels again: identity plate first, larger kit art second, and a compact spin preview only where editing needs it.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated `/profile/setup` route smoke, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.

## 2026-06-08

- Polished the Player Pass composition again with the `$gpt-taste` direction: removed the visible pin badge from the profile windows, made the left public profile card static, moved the spinning mascot into a dedicated right-side stage, and added visible emote tiles to the profile/loadout surfaces.
- Reworked the setup and public supported-country panels into a tighter bento layout with profile and kit tiles flush on the left, larger centered kit art, gold glow treatment, dedicated mascot stage, and no duplicate label clutter inside the mascot windows.
- Verified the profile composition polish with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated `/profile/setup` route smoke, in-app Browser login-redirect smoke, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Tightened the `/bracket` group drag preview so the floating team card tracks closer to the pointer instead of sitting down/right from the cursor.
- Normalized the extracted cloth flag exports to a consistent 3:2 crop and matched the bracket flag slots to the same ratio so Switzerland, Curaçao, Ecuador, Germany, Tunisia, Senegal, and the rest render centered/flush in group rows.
- Verified this quick bracket polish with `node scripts/extractWorldCupFlags.js`, in-app Browser drag/flag geometry QA on `/bracket`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`.
- Adjusted the Player Pass supported-country card so the selected mascot profile square sits on the left and the supported kit is centered on the right on both setup and public profile pages.
- Reordered profile customization to Mascot, Skins, Kit, Emote; removed the pin picker while keeping the saved/default pin as a display badge.
- Replaced the profile bracket empty-state trophy icon with the existing `public/assets/ui/trophy.png` asset.
- Verified the profile follow-up with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated `/profile/setup` route smoke, in-app Browser login-redirect smoke, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added finalized bracket results to profile pages by parsing saved `tournament_predictions.path` into champion, finalists, semi-finalists, quarter-finalists, third-place winner, Top 8 thirds, saved date, and completion state.
- Reworked `/profile/setup` into a tighter one-page Player Pass settings view: profile picture/skin preview with top-right pin, supported country badge, compact customization tabs for skins/mascots/kits/pins/emotes, kit card, and bracket-card panel.
- Reworked `/profile/[userId]` into the public-facing profile card requested: mascot-skin profile picture, pin, supported country kit, emote, stats, and finalized bracket picks without adding production GLB loading or fake bracket data.
- Verified the profile bracket-card update with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated route smoke checks for `/profile/setup` and `/login?message=Please%20sign%20in%20first`, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Fixed the `/bracket` group drag regression by removing the browser-native drag layer and keeping one custom pointer-drag path; both full-row drag and the visible `Drag` chip now reorder teams in Browser QA.
- Regenerated the extracted flag assets with tighter sheet crop slots and hardened the bracket flag slot styling so group flags render as consistent flush rectangles instead of uneven tiny stickers.
- Verified the fix with `npm.cmd run lint`, `npm.cmd run build`, and Browser QA on `/bracket` for handle drag reorder plus flag box geometry.
- Rebuilt the active profile UI from scratch as a Player Pass editor, replacing the profile overview/old locker lineage with a collectible pass preview, identity editor rail, and horizontal mascot/skin/kit/emote locker lanes.
- Reconnected `/profile/setup` to real `mascotLocker` state for the Canada/Mexico/USA mascot trio while preserving the existing `saveProfile` action, placeholder-only mascot rendering, separate kit identity, and emote selection.
- Rebuilt `/profile/[userId]` to match the Player Pass direction with the same lightweight mascot card language, public stats, kit/emote loadout, points breakdown, and mascot locker summary.
- Verified the blank-slate profile rebuild with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated route smoke checks for `/profile/setup` and `/login?message=Please%20sign%20in%20first`, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added a deterministic cloth-flag extraction pipeline at `scripts/extractWorldCupFlags.js`, copied the generated flag sheets into `public/flag-sheets/`, and exported exactly 48 official FIFA World Cup 2026 flag PNGs into `public/flags/world-cup-2026/`.
- Added canonical World Cup 2026 team/flag metadata in `lib/data/worldCupTeams.ts`, including aliases for official/team-provider naming differences such as Côte d'Ivoire, Curaçao, IR Iran, Korea Republic, Türkiye, USA, and Cabo Verde.
- Added `/flag-preview` as a lightweight visual inspection page for all 48 exported flags, with Denmark and Italy excluded.
- Updated `/bracket` so Groups, Top 8, knockout rows, and summary pills use the extracted cloth flag assets instead of provider logos or runtime sheet slicing.
- Changed the bracket setup UX so Top 8 only activates after the user clicks `Continue to top 8`; editing group order clears downstream Top 8/bracket selections and relocks the next step.
- Improved group dragging so the full team row starts the drag interaction, shows the floating team preview, and keeps the compact `Drag` affordance as a hint rather than the only active target.
- Browser-verified `/flag-preview` and `/bracket`: 48 preview cards/images, no Denmark or Italy, no horizontal overflow, extracted flags in Groups/Top 8/bracket, Top 8 gating, full-row Group A drag reorder, and trophy-gold knockout winner state.
- Verified with `node scripts/extractWorldCupFlags.js`, `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`; `perf:audit` is not defined, and World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Replaced the active `/profile/setup` first screen with a reference-style profile overview: large identity card, current avatar/kit panel, stats, profile details, quick actions, editable fields, blank `Not set` values, private email display, and display-name fallback from the signed-in email local part only.
- Kept the old mascot locker component available but no longer used as the first profile setup surface; no schema, provider, production 3D, betting/odds, or payment behavior changed.
- Verified this profile overview pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, and route smoke checks confirming signed-out `/profile/setup` still redirects to login.
- Replaced the active `/profile/setup` six-panel locker board with a cleaner matchday identity locker: editable display/crew/team fields, large selected mascot stage, mascot cards, skin picker, kit picker, emote picker, save action, and public-card link.
- Rebuilt `/profile/[userId]` away from the old share-card composition into a simpler public profile page with lightweight mascot art, stat tiles, kit/emote loadout, points breakdown, and mascot locker summary.
- Kept the profile revamp scoped to existing catalog-backed placeholder mascots, skins, kits, and emotes; no schema changes, production GLBs, model viewers, betting/odds language, or bracket files were touched.
- Verified the profile revamp with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, unauthenticated route smoke checks for `/profile/setup` and `/login?message=Please%20sign%20in%20first`, and profile-path scans for restricted terms plus 3D/model leakage. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Polished the active `/bracket` flow without changing the setup order: Groups still come first, Top 8 second, knockout third, and summary last.
- Replaced group ranking arrow controls with drag handles plus pointer-drop reordering and keyboard arrow support; rows now glow while dragging and target rows glow on drop.
- Upgraded Top 8 selected cards, knockout `Winner` rows, and the semi-final/third-place/final columns with stronger premium gold glow, while keeping the existing compact board layout.
- Added a share-ready HTML-style summary card for future profile/friends surfaces. It uses the real tournament payload to show champion, finalists, semi-finalists, quarter-finalists, third-place winner, and Top 8 thirds, with trophy imagery from `public/assets/ui/trophy.png`.
- Made the crown-stadium background a little more visible across the bracket hero and stage panels without sacrificing text contrast.
- Chrome DevTools QA verified drag reorder, Top 8 selection, 32/32 knockout completion, late-round styling, trophy watermark, and summary card rendering. Screenshots are saved in `artifacts/bracket-qa/`.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`; `perf:audit` is not defined in `package.json`. World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added the user-provided crown-stadium image as `public/assets/ui/bracket-crown-stadium-bg.png` and wired it into the `/bracket` hero, Groups, Top 8, knockout, summary, and side-panel backgrounds with darker overlays so the UI reads premium without hurting legibility.
- Improved `/bracket` UX after a fresh visual pass: step transitions now scroll to the active work surface instead of leaving users at the repeated hero, and the Top 8 side panel no longer stretches into a tall empty slab after the slot map.
- Browser-verified the updated bracket flow in the in-app tab: crown background is active on hero and stages, old stage background path is no longer used there, Top 8 and bracket transitions land on the active stage, winner rows still show trophy-gold `Winner` badges, and no horizontal overflow was detected at the current app viewport.
- Verified again with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`; World Cup validation still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Reverted the confusing `/bracket` guidance-heavy pass back to the simpler setup-first flow: Groups first, Top 8 second, then the knockout picker, without glowy jump helpers, next-missing shortcuts, feature-round wrappers, or the profile-ready summary card in the active UI.
- Kept the generated bracket-stage background asset wired into the bracket surfaces, and changed knockout selections so clicked teams show a clear trophy-gold row highlight plus a compact `Winner` badge instead of relying on a chevron/arrow cue.
- Browser-verified the rollback in the in-app `/bracket` tab: Groups opened first, Top 8 unlocked the bracket after eight picks, the generated background loaded, old helper/profile-summary classes were absent, and selecting Mexico in M73 produced the gold `Winner` state. Screenshot saved at `artifacts/bracket-qa/rollback-winner-state-final.png`.
- Verified the rollback with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`; the validator still only warns that 72 of 104 fixtures are synced and official knockout fixtures remain provider-pending.
- Added a generated bracket-stage background asset and wired it into the Groups, Top 8, knockout, and summary surfaces with stage-specific overlays.
- Added glowy guidance/jump helpers for overwhelming bracket areas: Groups now points to lower groups, Top 8 shows remaining third-place picks, and the knockout stage exposes the next missing match with a jump action.
- Replaced the visible full six-column knockout board at normal app widths with a round-focused tab/grid experience, removed the inner horizontal scrollbar, and added stronger quarter-final, semi-final, third-place, and crown-lit final visual treatment.
- Added a profile-ready tournament path summary card that uses the saved bracket payload to show champion, finalists, semi-finalists, third-place pick, and path depth for future profile/friends surfaces.
- Browser-verified Groups jump, Top 8 guidance, bracket round tabs, final glow state, completed-path summary unlock, generated background usage, and no horizontal overflow; verified again with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`.
- Polished the current `/bracket` UX again: added local trophy/kit imagery to the hero, made group rows promote on full-row click/keyboard focus, tightened Top 8 chip layout, switched normal-width knockout play to round-focused grids, added Top 8/champion confetti, and styled invalid-pick toasts to match the dark app UI.
- Browser-verified the updated bracket flow in the app shell at 1347x789: image assets loaded, the step rail stayed readable, Top 8 selection unlocked the bracket, pending Round of 16 clicks showed the expected prompt, and the bracket page avoided horizontal overflow.
- Tuned `/bracket` toward the generated match-night reference: compact full-path header, denser group cards, inline team marks, stronger navy glass panels, green auto-qualifier rows, gold third-place rows, and selected Top 8 glow/slot-map states.
- Verified the bracket visual pass with Browser screenshots at a 2048x1152 viewport, a Top 8 interaction loop, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build`; only stale Fast Refresh warnings remained in the dev log buffer after an intermediate hot-reload error.
- Reworked `/bracket` into a gated setup-first flow after visual QA: users now start on a full-width Groups step, continue to a full-width Top 8 third-place step, and only see the full knockout bracket after setup is complete.
- Replaced the cramped side-panel group/third-place controls with cleaner responsive grids, clearer step rail states, larger tap targets, compact rank controls, and a less confusing bracket unlock path.
- Forced `/bracket` to open on Groups even when a saved path exists, while still allowing users to jump ahead from the step rail once their setup is valid.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and an HTTP/server-render check confirming the first screen is the group setup.
- Rebuilt `/bracket` with an app-native full tournament predictor: group order controls, exactly eight third-place qualifier selection, FIFA 2026 Annexe C slot placement, click-to-advance knockout picks from M73-M104, M103 third-place support, winner highlighting, invalid-pick toasts, and a completion-gated summary.
- Connected the bracket screen to synced Supabase group/team data, signed-in saved tournament paths, and the existing `/api/predictions/tournament` save contract without adding schema, provider, payment, donation, betting, or odds behavior.
- Added responsive bracket behavior: full desktop board, mobile round focus tabs, compact side controls for groups/third-place/summary, and projected/lock status labels while official knockout fixtures remain provider-pending.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, HTTP 200 smoke check for `/bracket`, and server-render content checks for the new bracket shell; validator still warns only because 32 official knockout fixtures are not synced yet.
- Reset the `/bracket` route by removing the active bracket UI render and deleting the unused bracket UI component set, leaving the app shell and backend/data logic intact.
- Removed the latest scratch bracket CSS block from `app/globals.css`; `/bracket` now serves without `scratch-bracket`, `TournamentPathBuilder`, or `PremiumBracket` output.
- Verified the reset with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, and an HTTP smoke check for `/bracket`.
- Replaced the `/bracket` rendering surface with a clean scratch-built tournament path shell that uses only `scratch-*` UI components/classes for the active bracket page.
- Removed stale rendered reference artifacts from the bracket route: no implementation map, no code-ready placeholder panel, no old taste grid, and no old bracket-match-card component output.
- Kept the real 2026 tournament logic intact: 12 groups, exactly 8 third-place qualifiers, FIFA Annexe C third-place mapping, M73-M104 knockout path, M103 third-place match, M104 final, saved path support, and stale winner pruning.
- Tightened the bracket page frame to use the current app sidebar/background while fitting the desktop bracket shell inside one viewport without page scrolling.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, `npm.cmd run validate:worldcup`, and an HTTP smoke check for `/bracket`; validator still warns only because provider knockout fixtures are not synced yet.

## 2026-06-07

- Replaced the `/bracket` content area with the Figma-informed bracket builder shell while preserving the app background/sidebar, Supabase-backed saved tournament path, staged group/third-place flow, and current button styling.
- Added the full stage rail from Groups through Winner, a compact projected/official status strip, full knockout board context, active-stage editing, larger flag/code placeholders, team-select cells, winner dropdowns, and stable match/team data attributes for app integration.
- Added M103 third-place match support from the semi-final non-finalists, persisted `thirdPlaceWinner` through the tournament prediction path, and pruned stale downstream winners after upstream participant changes.
- Removed restricted visible wording from the points guide and desktop sidebar while keeping points-only scoring copy.
- Added tests for pruning invalid downstream winners and M103 stale-pick cleanup.
- Verified this bracket shell pass with `npm.cmd run lint`, `npm.cmd run test` (14 tests), `npm.cmd run build`, `npm.cmd run validate:worldcup`, `npm.cmd run check:connections`, and an HTTP smoke check for `/bracket`.
- Tightened the `/bracket` shell to open on the full Round of 32 board, move status chips into the board header, compress match cells into Figma-style two-row boxes with side dropdown controls, and fit the desktop bracket canvas into one viewport.
- Replaced the improvised bracket card/wizard surface with the approved blue reference container: same stage rail, right code-ready placeholder panel, bottom implementation map, compressed placeholder match cells, and no internal bracket scrolling on desktop.
- Replaced the reference-container attempt with an app-native TASTE bracket shell: useful right-side pick/progress controls, no code placeholder panel, no implementation map, cleaner one-screen lanes, and retained the existing tournament path logic.
- Tightened the `/bracket` full-path builder with focused knockout pagination, compact third-place labels, neutral pending slots, FIFA Annexe C auto-placement for third-place Round of 32 slots, and verified provider-backed tournament path scoring.
- Verified the bracket task with `npm.cmd run lint`, `npm.cmd run test` (12 tests), `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`; validator still warns that official knockout fixtures are not provider-synced yet.
- Added profile customization preview built around the original Maple, Zayu, and Clutch mascot trio.
- Added burner mascot skin catalog, kit catalog, emote catalog, unlock helpers, and mascot asset audit.
- Updated `/profile/setup` to show profile overview, mascot selection, skin preview drawer, separate kits, emotes-only collection, and public profile preview.
- Promoted the burner emote selector into a dedicated premium panel with selected-emote preview and builder navigation.
- Rebuilt the login redirect screen into a premium match-night auth gate with a mascot/profile preview stage.
- Tightened the profile builder command rail so active mascot/skin/emote state and save action are visible.
- Updated login page copy so redirected users know the profile builder includes mascot skins, kits, and emotes.
- Updated public profile rendering to use lightweight mascot-skin placeholder art instead of loading the mascot GLBs.
- Persisted selected burner skin and selected emote through existing `user_avatars` fields; no new Supabase schema was added.
- Verified with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:connections`, and `npm.cmd run validate:worldcup`.
- Added the project-local `world-cup-premium-profile-ui` skill and Aceternity registry metadata without installing new packages.
- Rebuilt `/profile/setup` into a six-panel premium profile locker board matching the requested mockup composition instead of hidden tabs.
- Replaced the weak mascot placeholder renderer with CSS-only premium mascot/skin, emote, and kit placeholder art; no production 3D models load in this profile path.
- Added burner profile pins and saved the selected pin through the existing avatar badge field, without adding a database migration.
- Updated public profile rendering to reuse the shareable profile preview card.
- Verified this profile UI pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, route smoke checks, and `npm.cmd run validate:worldcup`; `npm.cmd run check:connections` only failed because local Vercel CLI auth reported `Not logged in`.
- Removed the generic `/profile/setup` page header and replaced the tabbed profile builder with visible Profile Overview, Mascots & Skins, Skin Preview, Kits, Emotes, and Public Profile Preview panels.
- Added the inline desktop skin preview with Spin/Pause/Reset and Use Skin actions, and kept burner emote selection visible in the main board.
- Verified the corrected board pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, and unauth route smoke checks.
- Reworked the profile locker again using external locker UI references and a generated implementation reference: contained the builder at `max-w-[1500px]`, removed fixed-min-width column overflow, removed internal vertical scroller panels, and tightened the top/low row grid rhythm.
- Improved the CSS-only mascot placeholder art so Maple reads more like a bull mascot, Zayu as a jaguar mascot, and Clutch as an eagle mascot while still avoiding production GLB loading.
- Verified the reference-informed locker pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, unauth route smoke, and profile-path greps for 3D/model leakage and restricted gambling terms.
- Rebuilt `/profile/setup` into a fixed-height one-screen locker canvas matching the requested reference proportions: no separate command rail, explicit top/bottom grid rows, compact lower Kits/Emotes/Public Preview panels, emotes-only picker, and neutral CSS render slots for the original Maple, Zayu, and Clutch mascot placeholders.
- Verified the fixed-canvas profile pass with `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`, unauth route smoke checks, and profile-path scans for restricted gambling terms plus 3D/model leakage.
- Added repo context preservation workflow.
- Added docs memory structure.
- Added root `AGENTS.md` operating rules.
- Recorded repo inspection, large-file risks, and verification results.
- Added safe `.gitignore` rules for temp files, logs, generated public images, local mascot-lab model/texture payloads, and common 3D asset formats.
- Reduced full short-status output from 9,592 lines to 166 lines without deleting or untracking files.
- No production features changed in this task.
